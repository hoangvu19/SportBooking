# Backend environment example
# Copy this to .env and fill with real values (DO NOT commit your real .env to git)

# JWT
JWT_SECRET=replace_with_secure_jwt_secret

# Database (if used)
# DB_HOST=localhost
# DB_USER=sa
# DB_PASS=your_db_password
# DB_NAME=SportBookingDB

# SMTP (for OTP email sending)
# If you want the app to actually send emails, fill the SMTP_* values below.
# If these are not provided, the server will fall back to logging OTP codes to console (useful for local dev).
# SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=vupham.190504@gmail.com
SMTP_PASS=crev ibfj annq ndoj
SMTP_FROM="WhereWeSport" <no-reply@vupham.190504@gmail.com>

# Server port
PORT=5000
